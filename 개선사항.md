# 개선사항

이 문서는 "브라우저에서 테스트하고 개선할 점을 찾아달라"는 요청에 대한 결과입니다.
**이번 세션에는 Chrome 확장(브라우저 자동화 툴)이 연결되어 있지 않아 실제 클릭
테스트는 하지 못했습니다.** 대신:

1. `static/app.js`(1889줄) 전체를 처음부터 끝까지 다시 읽으면서 상태 전이
   (state transition)를 손으로 추적 — 탭 전환, 화면 전환마다 어떤 변수가
   바뀌고 안 바뀌는지 하나씩 확인했습니다.
2. `curl`로 실제 사이트(`103.204.13.68:8905`)를 호출해서 API 응답을 검증했습니다.
3. `node --check`로 문법, 정적 분석 스크립트로 미선언 변수를 확인했습니다.

이 방법으로 찾을 수 없는 것(레이아웃 깨짐, 실제 클릭 시 포커스/스크롤 체감,
모바일 화면 등)은 "미확인"으로 따로 표시했습니다. 아래는 우선순위순입니다.

---

## 🔴 심각 — 실제 오작동

### 1. 온라인 보기: "다음 화"가 스토리상 이전 회차로 이동함

**증상**: 온라인 보기 탭에서 회차를 열고 "다음 화"를 누르거나 웹툰 모드에서
아래로 계속 스크롤하면, 실제로는 더 **예전** 회차로 넘어갑니다. "이전 화"가
반대로 더 **최신** 회차로 넘어갑니다. 사용자 입장에서는 완전히 거꾸로
동작하는 것처럼 보입니다.

**원인**: `app/online.py`의 `list_episodes`는 사이트가 주는 순서를 그대로
반환합니다 — 이 사이트는 회차 목록을 **최신 회차부터** 보여줍니다(직접
확인함: "나루토" 1페이지가 `710화, 709화, 708화 ... 701화, 700화` 순으로
옴). 그런데 `static/app.js`의 온라인 리더 쪽 코드는 전부 "배열의 다음
인덱스 = 다음 화"라고 가정하고 있습니다:

- `gotoOnlineRelativeEpisode` (~1808번째 줄): `idx + direction`
- `onOnlineReachEnd` (~1655번째 줄): `episodes[idx + 1]`을 "다음 화"로 취급
- `onlinePagerStep` (~1779번째 줄): 위와 동일

배열 인덱스가 커질수록 회차 번호는 **작아지므로**, "다음"이라고 부르는
동작이 실제로는 "이전" 회차로 이동합니다.

(참고: 다운로드 탭/뷰어 탭은 이 문제가 없습니다. `crawler.crawl_toon`이
다운로드할 때 페이지별로 `reversed()`를 적용해서 오래된 순으로 다시
번호를 매기기 때문에, 뷰어가 읽는 로컬 회차 목록은 항상 오래된→최신
순서입니다.)

**제안하는 수정**: 온라인 리더의 "다음/이전" 개념을 배열 인덱스가 아니라
스토리 순서 기준으로 바로잡아야 합니다. 화면에 보여주는 **목록 자체는
최신순 그대로 두는 게 자연스러우니**(사이트 원래 모습과 같음), 대신 리더
안에서만 "다음 화" = 배열 인덱스 **감소**(`idx - 1`), "이전 화" = 인덱스
**증가**(`idx + 1`)로 세 군데(`gotoOnlineRelativeEpisode`,
`onOnlineReachEnd`, `onlinePagerStep`)를 한꺼번에 고치면 됩니다. 정렬 자체를
뒤집기보다 이쪽이 더 안전합니다 — 목록 화면 표시 순서는 그대로 두고 리더의
탐색 방향만 바꾸는 것이라 회귀 위험이 적습니다.

---

### 2. 탭을 안 거치고 로컬 리더 → 온라인 리더로 넘어가면 키보드 단축키가 두 번씩 실행됨

**증상**: (a) 뷰어 탭에서 리더를 열고 "목록으로"/"라이브러리"를 누르지
**않은 채로** 상단 탭에서 바로 "온라인 보기"를 클릭 → (b) 온라인 보기에서도
리더를 엶 → 이 상태에서 스크롤 키(Space, ↓, PageDown 등)를 누르면 **화면이
의도한 것보다 2배 더 스크롤**됩니다. 리더 모드 조합에 따라(한쪽은 웹툰
모드, 다른 쪽은 페이지 넘김 모드로 남아있는 경우) 화면에는 안 보이는
로컬 리더 쪽에서 조용히 `fetch` 요청까지 발생할 수 있습니다.

**원인**: 상단 탭 전환 핸들러(`static/app.js` 236~255번째 줄)는
`panelViewer.hidden`/`panelOnline.hidden`만 바꿀 뿐 `viewerState.view`나
`onlineState.view`를 리셋하지 않습니다. 그런데 키보드 단축키는 `document`에
등록된 **두 개의 독립적인 `keydown` 리스너**로 구현되어 있고, 각각
`if (viewerState.view !== "reader") return;`(1129번째 줄),
`if (onlineState.view !== "reader") return;`(1850번째 줄)로만 가드합니다.
"지금 그 패널이 실제로 보이는지"는 확인하지 않습니다. 그래서 리더를 연
채로 탭만 전환하면 `viewerState.view`가 `"reader"`로 남아있고, 이후 다른
쪽 리더를 열어 그쪽도 `"reader"`가 되면 두 리스너가 동시에 같은 키
입력에 반응합니다.

**제안하는 수정**: 두 `keydown` 가드에 패널이 실제로 보이는지 확인하는
조건을 추가합니다.

```js
// 로컬 리더 keydown 리스너
if (viewerState.view !== "reader" || panelViewer.hidden) return;

// 온라인 리더 keydown 리스너
if (onlineState.view !== "reader" || panelOnline.hidden) return;
```

한 줄씩 추가하는 것으로 끝나는 간단한 수정이고, 같은 종류의 문제를 막는
가장 일반적인 방법이라 다른 곳에 비슷한 버그가 있어도 함께 예방됩니다.

---

## 🟡 중간 — 오작동이지만 영향은 제한적

### 3. 페이지 넘김 모드에서 이미지가 0장인 회차를 열면 빈 화면만 보임

`renderPagedImage`/`renderOnlinePagedImage`는 `images.length === 0`이면
아무 안내 문구 없이 그냥 `return`합니다(각각 954번째 줄, 1742번째 줄).
사이트 쪽 문제로 이미지 목록이 비어 있는 회차를 열면 사용자는 로딩이 끝난
건지, 회차에 이미지가 없는 건지 구분할 수 없습니다. 웹툰 모드는 그나마
회차 구분선에 "0장"이라고 표시되어 상황을 알 수 있는 것과 대비됩니다.
→ `images.length === 0`일 때 `pagerBox.innerHTML = '<div class="empty-state">이 회차에는 이미지가 없습니다.</div>'` 같은 처리를 추가하는 게 좋겠습니다.

### 4. 온라인 보기 "다음 페이지" 버튼이 마지막 페이지에서도 계속 활성 상태

`renderOnlineEpisodeList`(1380번째 줄)는 "이전 페이지"는 `page <= 1`일 때
비활성화하지만, "다음 페이지"는 서버가 총 페이지 수를 안 줘서 항상
활성화된 채로 둡니다. 회차가 없는 페이지까지 계속 눌러볼 수 있는데, 큰
문제는 아니지만("이 페이지에는 회차가 없습니다" 안내는 뜸) 빈 페이지에서
버튼을 계속 활성 상태로 두는 건 약간 어색합니다. → 응답으로 받은
`episodes`가 빈 배열이면 그 화면에서 "다음 페이지" 버튼을 비활성화하는
정도의 휴리스틱은 추가할 수 있습니다.

### 5. 탭을 벗어나도 IntersectionObserver가 백그라운드에서 계속 동작

리더를 연 채로 다른 탭으로 전환해도 `sentinelObserver`/`positionObserver`가
`disconnect()`되지 않습니다(2번 항목과 같은 원인 — 탭 전환 핸들러가 리더
상태를 정리하지 않음). 화면에 안 보이는 요소는 `isIntersecting`이 항상
`false`라 자동으로 추가 로드가 계속되지는 않지만, 옵저버 자체는 다음에 그
리더를 다시 열어 `teardown...Observers()`가 호출되기 전까지 메모리에
남아있습니다. 심각하지 않지만 2번을 고치는 김에 탭 전환 시
`teardownReaderObservers()`/`teardownOnlineReaderObservers()`도 같이
호출해주면 깔끔합니다.

### 6. 페이지 이동 버튼 연타 시 경쟁 상태(race condition) 가능성

`openOnlineEpisodes`, `openTitle` 같은 함수들은 진행 중인 요청이 있어도
그 위에 새 요청을 또 보낼 수 있습니다(로딩 가드 없음). "다음 페이지"를
빠르게 두 번 누르면 늦게 도착한 응답이 최신 화면을 덮어쓸 수 있습니다.
로컬 도구 특성상 실사용에서 자주 부딪힐 상황은 아니라 우선순위는 낮게
뒀습니다.

---

## 🟢 사소 — UX 제안 (버그 아님)

### 7. 온라인 보기 탭 첫 진입 시 사이트 주소가 비어 있음
다운로드 탭에 이미 사이트 주소를 입력해 둔 상태라면, 온라인 보기 탭을 처음
열 때 그 값을 자동으로 채워주면 다시 타이핑할 필요가 없어집니다.

### 8. 검색어를 지우고 검색하면 결과 영역이 안내 없이 그냥 비워짐
다운로드 탭·온라인 탭 검색 모두 해당. 빈 검색어일 때 "검색어를 입력하세요"
같은 문구를 보여주면 "결과가 사라진 게 버그인가?" 하는 혼란을 줄일 수
있습니다.

### 9. (미확인) 좁은 화면에서 리더 상단바 버튼 줄바꿈
리더 상단바에 버튼이 최대 9개(쪽 이동, 1·2쪽, 방향, 보기모드, 전체화면,
라이브러리, 목록으로, 이전 화, 다음 화)까지 나열될 수 있습니다.
`.reader-nav`에 `flex-wrap: wrap`이 걸려 있어 좁은 화면에서는 줄바꿈되긴
하겠지만, 실제 모바일 폭에서 얼마나 자연스러운지는 브라우저로 직접 봐야
압니다.

---

## 확인했지만 문제없었던 것들 (참고)

- 검색 → 회차 목록 → 이미지 URL까지 전체 체인을 curl로 재확인(나루토
  id=4, 천년영웅 id=37045) — 정상.
- `[hidden]` 관련 CSS, `.search-results` 클래스 등 이전에 잡은 버그들은
  재발 없음.
- 로컬 리더(`#reader-*`)와 온라인 리더(`#online-reader-*`) DOM id 충돌
  없음(전수 grep 확인).
- 정적 분석으로 미선언 변수·미정의 함수 참조 없음 확인.

---

## 다음 단계 제안

1번과 2번은 실제로 사용자가 부딪힐 가능성이 있는 버그라 먼저 고치는 걸
추천합니다. 원하시면 지금 바로 반영하겠습니다 — 두 개 다 각각 몇 줄
수준의 수정입니다. 3~6번도 원하시면 같이 처리할 수 있습니다.
